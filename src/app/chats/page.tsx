'use client';

import React from 'react';
import ChatList from '@/components/chat/ChatList';
import { type Lang } from '@/translations';

export default function ChatsPage() {
    return <ChatList lang="en" />;
}
